{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug SwiftUtilities",
            "program": "${workspaceFolder}/.build/debug/SwiftUtilities",
            "args": [],
            "cwd": "${workspaceFolder}",
            "terminal": "integrated",
            "preLaunchTask": "swift-build"
        }
    ]
}